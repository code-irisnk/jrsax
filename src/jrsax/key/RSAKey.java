package jrsax.key;

/**
 * Marker interface for RSA keys (either public or private).
 * <p>
 * This interface can be used to represent a common type for both {@link RSAPublicKey} and {@link RSAPrivateKey}.
 */
public interface RSAKey {
}
